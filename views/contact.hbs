
<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Contact</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    
    <h1>Contact</h1>
    <p>Send me an email at programmer@example.com</p>
    <h2>Contact me</h2>

    {{#if isSent}}
      <div class="isSent">The message has been sent!</div>
      {{#if imageBase64}}
        <img src="data:image/png;base64,{{imageBase64}}" alt="Uploaded Image" width="200" height="200">
      {{/if}}
    {{/if}}

    {{#if isError}}
      <div class="isError">You can't leave fields empty!</div>
    {{/if}}
        
    <form action="/contact/send-message" method="POST" enctype="multipart/form-data">
      <label>
        Your name: <input type="text" name="author">
      </label>
      <label>
        Your email: <input type="email" name="sender">
      </label>
      <label>
        Title: <input type="text" name="title">
      </label>
      <!-- ... -->
      <input type="file" name="projectDesign" accept=".jpg, .jpeg, .png, .gif" />
      <!-- ... -->
      <label>
        Message: <textarea name="message"></textarea>
      </label>
      <button type="submit">Send message</button>
    </form>

    {{#if isSent}}
      <div class="isSent">
        <p>The message has been sent! File {{ fileName }} has been saved!</p>
      </div>
    {{/if}}

    {{#if isError}}
      <div class="isError">You can't leave fields empty!</div>
    {{/if}}
    
  </body>
</html>

